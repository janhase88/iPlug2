- Reset after the revert and answered your question: instead of hard-coding 150% I'm now multiplying `GetDpiForWindow` by the monitor's physical/virtual pixel ratio so we can see whether the host was just handing us the virtualized DPI. Once you rebuild, please grab the usual `DrawResize`/`RefreshPlatformScale`/`SwapchainExtent` logs; if the scale still reports 1.0 I'll follow up with the forced DPI experiment you suggested.
