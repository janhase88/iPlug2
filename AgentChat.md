- Reset after the revert and answered your question: instead of hard-coding 150% I'm now multiplying `GetDpiForWindow` by the monitor's physical/virtual pixel ratio so we can see whether the host was just handing us the virtualized DPI. Once you rebuild, please grab the usual `DrawResize`/`RefreshPlatformScale`/`SwapchainExtent` logs; if the scale still reports 1.0 I'll follow up with the forced DPI experiment you suggested.

Just to answer your latest question: I did the reset intentionally. After we spent hours tweaking the window sizing without improving sharpness, I parked the previous experiments on a backup branch and went back to a clean baseline so I could re-derive the DPI reading logic. Nothing else mysterious happened—no upstream force-push—just me trying to start from a known state before testing the monitor-ratio approach.

Current snapshot: monitor-ratio DPI reporting is in, but the logs still show the host feeding us 1.0 and the Skia surface stays blurry. We learned the swapchain happily resizes yet never produces higher-res textures. Next I’ll hard-wire the render path to treat the device pixel ratio as 1.5 inside Skia/Vulkan so we can prove whether the backend honors a forced DPI; if that doesn’t move the needle we’ll dive deeper into the Skia image provider or the Vulkan renderpass setup.

- Follow-up: I wired that “dive deeper” pass. Every DPI query now logs the host DPI, virtualization ratio, and effective scale via `DBGMSG`, the Skia bitmap/surface creation paths dump their logical vs render pixels, and the Vulkan swapchain logs its requested vs clamped extent with verbosity forced to info/debug. Please rebuild with the Windows VST3 + Skia/Vulkan config, run at 100 % and 150 %, and paste the new backend/Skia/Vulkan log lines so we can see where the pixels get downscaled.

- New update after the latest deep dive: I split the host DPI from the physical render DPI in `IGraphicsWin`, so the host still sees the virtual 1.0 scale while the renderer allocates at the measured physical scale. The window now resizes off the host scale, the swapchain paths see the physical pixels, and every DPI refresh logs both values. Rebuild and let me know if the swapchain extent finally jumps to 1800×750 at 150 %, or paste the logs so I can chase the next bottleneck.

- Fresh pass: took your advice to stop nibbling and go end-to-end. Windows now caches both the host layout scale and the physical render scale, `SetScreenScale()` only feeds the host value into layout math, and Skia/Vulkan ask `GetBackingPixelScale()` for the backing resolution so they allocate the swapchain at the physical DPI. The HWND tree should stay at the DAW’s logical size while `DrawResize`/`SwapchainRequest` report the higher render scale. Please rebuild at 150 %, confirm the child window no longer overflows, and paste the latest `RefreshPlatformScale` + `DrawResize` + `SwapchainRequest` lines so I can verify the render target hits 1800×750 without Windows re-virtualizing it.
