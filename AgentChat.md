- Reverted the previous attempt that forced physical swapchain extents because it destabilized the build and still produced blurry output. We're now back on the pre-experiment baseline (commit 84b036e) so the editor behaves exactly as before the regression.
- Next step is to revisit the DPI investigation with a fresh plan once we have a reproducible strategy that won't resize the host window incorrectly.
