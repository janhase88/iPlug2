- I rolled back the swapchain experiment because it broke drawing (missing backgrounds, overflows) yet never delivered a sharp frame, so we need a clean baseline before trying again.
- New plan: keep that baseline in place, then reapply the promising pieces one by one with validation after each step—first confirm Skia surfaces actually receive the physical DPI via hard-coded overrides, then audit the Vulkan renderpass/attachments to guarantee they allocate with the same pixel extent, and only after that revisit the window/swapchain negotiation.
- Once each stage proves it really changes raster resolution without distorting layout, we can commit the fixes; if any step still blurs, we stop there and inspect the backend (Skia image provider vs. Vulkan renderpass) instead of blindly layering more changes.
